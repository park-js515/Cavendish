spring:
  profiles:
    active:
      - local
    group:
      local:
        - db-local
        - log-local
      prod:
        - db-prod
        - log-prod
    include:
      - db
      - jwt
      - log
      - server
      - swagger
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher